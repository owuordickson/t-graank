language: python

python:
    - "3.11"

before_install:
  - sudo apt-get -y install libopenmpi-dev
  - "pip install ."

install: pip install -r requirements.txt

script: python3 src/cli_main.py -f datasets/DATASET.csv
