language: python

python:
    - "3.6"

before_install:
  - sudo apt-get -y install libopenmpi-dev
  - "pip install ."

install: pip install -r requirements.txt

script: python3 src/cli_tgraank.py -f datasets/DATASET.csv
